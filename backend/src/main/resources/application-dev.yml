# Development-only overrides for verbose logging and SQL visibility.
spring:
  jpa:
    show-sql: true

logging:
  level:
    com.animetracker: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE
